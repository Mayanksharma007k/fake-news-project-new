import {Activity, Award, FileCheck2, Link2} from "lucide-react"; import {ledger} from "@/lib/mockData"; import StatusBadge from "@/components/StatusBadge"; import Link from "next/link";
export default function Dashboard(){return <main className="container py-10"><h1 className="text-3xl font-bold">Your Dashboard</h1><p className="muted mt-2">Your verification and community activity.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[[FileCheck2,"Articles checked","24"],[Activity,"Community reviews","18"],[Award,"Reputation score","720"],[Link2,"Blockchain records","9"]].map(([I,a,b]:any)=><div className="card p-5" key={a}><I size={20}/><div className="muted mt-4 text-sm">{a}</div><div className="mt-1 text-2xl font-bold">{b}</div></div>)}</div><div className="card mt-5 p-6"><h2 className="text-xl font-bold">Recent verifications</h2><div className="mt-4 space-y-2">{ledger.slice(0,4).map(x=><Link href={`/results/${x.id}`} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-slate-800 p-4 hover:bg-slate-900" key={x.id}><span className="max-w-2xl text-sm">{x.claim}</span><StatusBadge status={x.status}/></Link>)}</div></div></main>}